1. Returns a pointer to the resulting string
2. The _strncat function is similar to the _strcat function
3. Your function should work exactly like strncpy
4. Your function should work exactly like strcmp
5. 
6. Where n is the number of elements of the array
7. function that changes all lowercase letters of a string to uppercase.
8. Separators of words: space, tabulation, new line
